services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL:?error}
      NEXTAUTH_URL: ${NEXTAUTH_URL:?error}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:?error}
